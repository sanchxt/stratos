## backend commands:

- > cd backend
- > pnpm install
- > npx prisma generate
- > npx prisma migrate dev --name init
- > pnpm dev

## frontend commands:

- > cd extension
- > pnpm install
- > pnpm dev
